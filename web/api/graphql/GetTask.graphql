query GetTask($id: ID!) {
  task(id: $id) {
    id
    title
    description
    done
    dueDate
    checksum
    patient {
      id
      name
    }
    assignee {
      id
      name
    }
    properties {
      definition {
        id
        name
        description
        fieldType
        isActive
        allowedEntities
        options
      }
      textValue
      numberValue
      booleanValue
      dateValue
      dateTimeValue
      selectValue
      multiSelectValues
    }
  }
}