query MyQuery {
  patients {
    age
    assignedLocationId
    birthdate
    firstname
    gender
    id
    lastname
    name
    properties {
      definition {
        allowedEntities
      }
    }
    tasks {
      id
    }
  }
}
